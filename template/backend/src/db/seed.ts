// No seed data needed – app_users are auto-provisioned on first login via JWT exchange.
// Add app-specific seed data here if needed.
export async function seedDefaults(): Promise<void> {
  // nothing to seed
}
