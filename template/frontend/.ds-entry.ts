// Design-system bundle entry for /design-sync.
// Re-exports the public component barrel so window.ConvergeUI mirrors the real
// app-template public API (including the `DropdownMenu` namespace export).
export * from './src/components/ui';
